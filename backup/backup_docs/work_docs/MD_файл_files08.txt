Три файла выданы (каждый выкладывался сразу после сборки)

Файл	Модуль	Ур.	Оценка
I4_drafts_translate.md	drafts/translate.py	Mid	170 LOC / 10 тестов
F1_watchdog_memory.md	watchdog/memory.py	Mid+	230 LOC / 12 тестов
G1_log_redactor.md	security/redactor.py	Mid	190 LOC / 12 тестов

Решения, принятые в этих трёх (не следуют из спеки дословно)

Где	Решение	Обоснование
I4	Перевод черновика с признаком дрейфа не прикрепляется вообще (None), а не помечается	Пометку в UI прочитают не всегда, а текст скопируют в чат клиенту
I4	Контекст предыдущих сегментов не передаётся	Иначе факты из разговора попадут в текст, проверенный только против библиотеки
F1	Режим vmrss считается деградировавшим: WARNING один раз + флаг в snapshot()	Без дочернего whisper (550–700 МБ) счётчик врёт в меньшую сторону и защита от OOM молча выключается
F1	psutil не используется	/proc и cgroup читаются штатно; лишняя зависимость в MVP
G1	exc_info принудительно форматируется в exc_text, exc_info обнуляется	Иначе форматтер соберёт трейс заново из объекта исключения и обойдёт фильтр — сообщение чистое, трейс с ключом

Ограничения выполнения

В I4 осталась незакрытая развилка, помеченная в разделе «Подсказки»: базовый класс D1 уже прогоняет аудит и кладёт находки в result.changes, поэтому повторный вызов detect_drift в I4 избыточен. Исполнителю указано оставить один путь, но какой именно — я не зафиксировал. Правильнее решить это до кодирования: предлагаю читать result.changes, а detect_drift в I4 не вызывать.
Порог [memory].high_mb в конфиге и MemoryHigh= в systemd-unit — два независимых места; проверку их соответствия я отнёс к старту приложения (main, ✅ реализован), то есть это правка уже готового модуля, а не часть F1.
Формы ключей в DEFAULT_PATTERNS (G1) — по состоянию на май 2026, достоверность ~85 %. Литералы через add_literal покрывают случаи, которые паттерны пропустят.

✓ Партия 3: I3, I4, F1, G1 готовы

⧗ Контракты: закрыто 14 из 23, осталось 9 — следующий G2 (BYOK, строго после приёмки G1), затем G3/G4/E1/E2/E3/B1/B3/H6
