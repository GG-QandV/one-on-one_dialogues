Объединяю малые задачи (D6→TRANSLATE подключение + CONTRACTS-фиксация + сверка `catch_up`) в один патч. I3/I4/DRAFT-обработчик не трогаю — это полноценные модули, не «мелкие».

Читаю D6 `context.py`, чтобы патч подключения был точным.

Синтезировал четыре задачи в единый патч с миграцией

Синтезировал четыре задачи в единый патч с миграцией

D6 API ясен: `build_context(db, segment_id, cfg) -> tuple[str, ...]`, чистое чтение. Но при чтении всплыли **две вещи, которые надо признать честно**:

1. **Индекс.** D6-запрос (и мой JOIN) бьёт по `(stream_id, track, t_start_ms)` — такого составного индекса в схеме нет (есть `(stream_id, t_start_ms)` без track). На часовой сессии это скан. Нужна миграция индекса.
2. **Мой файл миграции `0007_...` был назван неверно.** `migrate()` матчит glob `[0-9][0-9][0-9]_*.sql` (ровно 3 цифры) и парсит `int(name[:3])`. Имя `0007_draft_confidence.sql` (4 цифры) под glob **не попадает** — миграция бы не применилась. Раз DRAFT прошёл (283 passed), у тебя имя уже поправлено на 3 цифры. Подтверди номер, чтобы новый файл встал следующим.

Собираю объединённый патч.

Created 2 files, read a file

Created 2 files, read a file

Пакет малых задач готов — один патч, четыре задачи, каждая со спекой.

| Задача          | Тип      | Суть                                                                         |
| --------------- | -------- | ---------------------------------------------------------------------------- |
| 1. D6→TRANSLATE | код      | `build_context` вместо `context=()` + `ContextConfig` в `__init__` + 2 теста |
| 2. Индекс       | миграция | `(stream_id, track, t_start_ms)` под горячий путь D6/JOIN                    |
| 3. catch_up     | код      | сверить в `start_session`, при отсутствии добавить                           |
| 4. CONTRACTS    | junior   | реестр в ✅ + 3 отклонения зафиксировать + убрать заглушки                    |

**Две честные поправки, всплывшие при чтении:**

1. **Индекса под D6-запрос в схеме нет** — на часовой сессии это скан. Миграция добавлена.
2. **Мой прежний файл `0007_draft_confidence.sql` назван неверно** — `migrate()` матчит ровно 3 цифры (`[0-9][0-9][0-9]_`), 4-значное имя под glob не попадает и не применилось бы. Раз DRAFT прошёл (283 passed), у тебя имя уже поправлено. Новый индекс-файл назвал `009_` (3 цифры), но номер — предположение: **сверь последовательность в `migrations/` и переименуй под следующий свободный**.

Три отклонения для CONTRACTS (§12↔промпт язык, мягкий `strict_numbers`, C3 second fallback) выписаны в задаче 4 — junior просто переносит.

Одно нужно от тебя перед применением задачи 2:

Номер индекс-миграции (сейчас 009 как предположение):Агент сверит сам при применении

Принято — агент сверит номер по `migrations/` и переименует под следующий свободный (3 цифры). Патч уже это оговаривает в задаче 2 и в шапке файла миграции.

Пакет готов к применению целиком. Сводка того, что уходит агенту:

**Применить (`apply/`):**

* `PATCH_small_batch.md` — 4 задачи со спеками
* `migrations/009_context_index.sql` — переименовать под свободный номер

**Порядок:** индекс → D6-подключение (+2 теста) → сверка catch_up → прогон (ожидание 283 + новые) → CONTRACTS (junior, параллельно).

Что остаётся открытым после этого пакета — крупные модули, не мелочь:

| Модуль                | Блокирует            | Готовность |
| --------------------- | -------------------- | ---------- |
| I3 trigger            | DRAFT-обработчик     | ⬜          |
| DRAFT-обработчик      | вкладку черновиков   | ждёт I3    |
| I4 перевод черновиков | двуязычные черновики | ⬜          |
| E4 вкладка черновиков | UI черновиков        | ждёт I3/I4 |

Цепочка черновиков (`I3 → DRAFT-обработчик → I4 → E4`) — логичный следующий крупный блок, начинать с I3, когда будешь готов.

---

## 009 context index · SQL

-- Индекс под горячий путь: D6 build_context и TRANSLATE-обработчик
-- фильтруют segments по (stream_id, track, t_start_ms). Без составного
-- индекса — скан таблицы к середине часовой сессии.
--
-- ВАЖНО про имя файла: migrate() матчит glob [0-9][0-9][0-9]_*.sql (РОВНО
-- 3 цифры) и парсит int(name[:3]). Номер ниже (009) — ПРЕДПОЛОЖЕНИЕ.
-- Сверить с фактической последовательностью в migrations/ и переименовать
-- под следующий свободный НОМЕР ИЗ ТРЁХ ЦИФР перед применением.

CREATE INDEX IF NOT EXISTS idx_segments_context
    ON segments(stream_id, track, t_start_ms);
