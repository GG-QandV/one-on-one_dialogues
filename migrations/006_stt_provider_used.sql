-- STT-провайдер, фактически распознавший сегмент (диагностика E5/истории).
-- Нужен, чтобы после инцидента с падением облака было видно, какие сегменты
-- ушли через local_whisper с более низким качеством распознавания.
-- NULL для track='fast' и для сегментов, записанных до этой миграции.

ALTER TABLE segments ADD COLUMN stt_provider_used TEXT;
