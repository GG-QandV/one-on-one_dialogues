-- Конкретное звено STT-цепочки, распознавшее сегмент, с учётом ключа
-- (напр. "custom_api:groq_0"). Отличает ключи одного провайдера — при
-- ротации/падении ключа видно, какой именно сработал.
-- NULL для track='fast' и сегментов до этой миграции.

ALTER TABLE segments ADD COLUMN stt_entry_used TEXT;
