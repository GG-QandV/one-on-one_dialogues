"""Критерий 5 (§21): задержка точного трека ≤ 3000 мс, медиана по 50 репликам.

LIVE — нужно живое железо и 50 реальных реплик. Задержка меряется от
КОНЦА произнесения (t_end_ms), а не от начала сегмента (H2, ловушка).
"""

from __future__ import annotations

from tests.acceptance.harness import live_stub

CHECK = live_stub(
    5,
    "задержка точного трека ≤ 3000 мс (медиана, 50 реплик)",
    "Руками: записать сессию из 50+ реплик, для каждой замерить время от "
    "t_end_ms сегмента до появления перевода (событие segment.translated), "
    "построить гистограмму и медиану. Порог 3000 мс — из спеки, не подгонять.",
)
