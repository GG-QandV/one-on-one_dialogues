"""Критерий 2 (§21): раздельный захват узла встречи и микрофона одновременно.

LIVE — нужен реальный PipeWire с двумя активными источниками звука.
"""

from __future__ import annotations

from tests.acceptance.harness import live_stub

CHECK = live_stub(
    2,
    "раздельный захват узла и микрофона одновременно",
    "Руками: запустить сессию с двумя активными источниками (микрофон + звук "
    "встречи), вызвать diagnose() для обоих потоков (app/audio/capture.py), "
    "подтвердить ненулевой RMS в обоих одновременно.",
)
