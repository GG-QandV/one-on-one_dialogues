"""Критерий 6 (§21): черновик быстрого трека ≤ 1500 мс, замещается проверенным.

LIVE — нужно живое железо и облачный быстрый трек. Проверяется по событиям
SSE и полю superseded_by_segment_id (H2, карта критериев).
"""

from __future__ import annotations

from tests.acceptance.harness import live_stub

CHECK = live_stub(
    6,
    "черновик быстрого трека ≤ 1500 мс, замещение проверенным",
    "Руками: замерить время от начала реплики до draft.created (порог 1500 мс), "
    "затем подтвердить, что после появления точного перевода происходит "
    "supersede (SQL: superseded_by_segment_id проставлен). Порог из спеки, не подгонять.",
)
